name: clickgas_core
description: Shared code for the ClickGas customer and driver apps (models, Supabase, theme, auth).
publish_to: 'none'
version: 1.0.0

resolution: workspace

environment:
  sdk: ^3.13.3

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.17.2
  google_fonts: ^8.2.1
  google_maps_flutter: ^2.18.0
  geolocator: ^14.0.3
  shared_preferences: ^2.5.5
  flutter_local_notifications: ^22.3.0
  image_picker: ^1.2.3
  firebase_core: ^4.14.0
  firebase_messaging: ^16.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
